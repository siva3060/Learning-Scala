
/**
 * Description : writing the code more consise
 * Author : Siva Charan
 * email : siva3060@gamil.com
 *
 */

val flag : Boolean = false
val result : Boolean = (flag == false)

// 1.we will use type inference in scala to take of the types 
// 2.we can use the value of flag/newflag  using the NOT operator

val newflag = false
val newresult = (!flag)

println(newresult)

